<UserControl x:Class="ShoppingBasketUi.UI.Product"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShoppingBasketUi.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid MinHeight="100">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Name="ProductDescription" IsReadOnly="True" AcceptsReturn="True" VerticalContentAlignment="Top" HorizontalContentAlignment="Left"></TextBox>
        <Button Name="Addbuton" Grid.Column="1" Click="Button_Click">Add to basket</Button>
    </Grid>
</UserControl>
